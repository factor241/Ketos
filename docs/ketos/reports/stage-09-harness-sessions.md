# Отчёт этапа 9. Сессии Harness в окнах

> Заполнен по шаблону [stage-report-template.md](../stage-report-template.md). Ветка `stage-09-harness-sessions`, worktree `/Volumes/Projects/Ketos bot.worktrees/stage-09` (от принятой ветки этапа 8 `stage-08-layout-persistence`; подготовительный коммит `971857a` объявил `bindings` в схеме `ui-board`). План этапа — `ketos_v7_master_plan/stage-09-harness-sessions.md` (ревизия 17); задачи — Beads `ketos-5v2.10.2`–`ketos-5v2.10.4`, эпик `ketos-5v2.10`.

**Демонстрация (живой прогон).** Скриншоты `.playwright-mcp/stage-09-gif/qa/90-restored-windows.png` (восстановление после перезагрузки) и `91-missing-session.png` (пропавшая сессия и её действия), машиночитаемые проверки — `.playwright-mcp/stage-09-gif/audit/audit.json`, вердикт — `audit-verdict.json` `{ total: 8, failed: [], ok: true, aborted: false }`. Прогон — живой сервер `pnpm ketos web --port 3099` против scratch-дома `DSH_HOME=/tmp/ketos-stage09-home` без провайдера моделей (модельные раунды не выполнялись; один промпт зафиксировал ошибку хоста «no API key», чтобы получить непустой чат для правила дублей). GIF не записывался: этап не меняет геометрию, шкалу и слои (аудит §4.10 для таких этапов не требуется), а PR ещё не создавался; каталог этапа с проверками и кадрами сохранён в worktree.

## 1. Итог этапа

Окна переживают перезагрузку страницы и перезапуск процесса: карта `windowId → sessionId` пишется в закладку `bindings` секции `ui-board` на дискретных событиях (создание, перепривязка, закрытие), восстанавливается при каждом принятии раскладки (кэш первого кадра и серверный документ), а `sessions.list` стал источником согласования — окно само привязывается к перечисленной сессии, честно показывает состояния «восстановление» и «сессия пропала» с действиями «создать» и «выбрать чат» и возвращается к жизни, если сессия появилась снова. Введено правило «одна сессия — одно окно»: `bind` возвращает различимый результат (`bound`/`same`/`duplicate`/`unknown`), выбор чата из панели или OmniBox поднимает уже показывающее его окно, OmniBox получил список недавних чатов с заголовком и каталогом, а проектная пустая сессия переиспользуется только пока её не показывает другое окно. Закрытие окна освобождает подписки и не удаляет сессию; поздние результаты create/fork не воскрешают закрытое окно и не перетирают выбор пользователя. Решение этапа — Agent Note [2026-09-19-ketos-board-session-restore.md](../../../.agents/notes/implemented/architecture/2026-09-19-ketos-board-session-restore.md).

## 2. Подэтапы

| Подэтап | Задача Beads | Статус | Подтверждение |
|---|---|---|---|
| 9.1 Восстановление сессий из сохранённой раскладки | `ketos-5v2.10.2` | выполнен | `BoardSettings` несёт `bindings`; `BoardLayoutPersistence` — единственный писатель секции и мержит живую карту в каждый патч; `BoardSessionBridge.restore()` принимает пары для окон восстановленной раскладки, чистит дубли и пары исчезнувших окон и сверяется со списком; применённый adoption-слушатель вызывается и для кэша (до первого кадра), и для серверного документа, поэтому окна привязываются до монтирования. Живой прогон: две пары в `settings.yaml`, перезагрузка вернула оба окна к своим сессиям, новых сессий не создано. Тесты: две пары восстанавливаются с одним `open` на пару, `restoring→ready`, adopt-level сценарий в `apply.client.spec.tsx`, кэш-раунд-трип и один патч на оба полу-документа в `board-persistence.client.spec.ts` (`tests/session-bridge.client.spec.ts`, `tests/apply.client.spec.tsx`, `tests/board-persistence.client.spec.ts`) |
| 9.2 Открытие существующих сессий и защита от дублей | `ketos-5v2.10.3` | выполнен | `bind` возвращает `BoardBindOutcome`, apply центрирует окно-держателя при `duplicate`, панель сообщает локализованное «чат исчез» при `unknown`; `startChat` не переиспользует пустую сессию, которую показывает другое окно; OmniBox получил секцию «Недавние чаты» (шесть последних по времени, заголовок и каталог) и `openChat` с тем же правилом (фокус существующего окна, а не второе окно). Живой прогон: выбор чата чужого окна в панели поднял окно-держателя и не изменил `bindings`; пункт «привет · stage-09» не открыл третье окно. Тесты: duplicate/same/unknown, независимая привязка разных чатов, пустая сессия проекта, повторный выбор фокусирует (`tests/session-bridge.client.spec.ts`, `tests/omnibox.client.spec.tsx`) |
| 9.3 Жизненный цикл, ошибки и утечки | `ketos-5v2.10.4` | выполнен | Согласование по `sessions.list`: отсутствующая сессия → `missing` без удаления окна (производное состояние очищено, заголовок и каталог сохранены), вернувшаяся — снова `ready`; действия «Новая сессия»/«Выбрать чат» в лейне; dock показывает error. Дублированная подписка на `permissions` снята (осталась одна проекция + отдельная свёртка строк). Утечки: закрытый рекорд не отвечает на изменения мёртвой сессии (канал заморожен), циклы открытия-закрытия и перепривязки не растят карты и число созданий, dispose не пишет карту. `pickWorkspace` и мёртвого пути больше нет в коде (удалены этапом 4, зафиксировано в отчёте). Тесты: missing/возврат/восстановление через `startChat`, гонки «закрытие во время create» и «выбор чата во время create», циклы утечек (`tests/session-bridge.client.spec.ts`), состояния лейна (`tests/conversation-body.client.spec.tsx`); живой прогон удалил файл сессии при остановленном хосте и подтвердил состояние и восстановление |

## 3. Критерии приёмки этапа

- [x] Восстановление сессий работает от сохранённой раскладки — живой прогон (`audit.json` `90-write-bindings-on-create`, `90-reload-restores-sessions`, `93-no-new-session-on-restore`) и `tests/apply.client.spec.tsx` (принятая секция открывает обе сессии до монтирования окон).
- [x] Дубли окон исключены (решение задокументировано) — правило «одна сессия — одно окно» и его альтернативы зафиксированы в Agent Note и README (en/zh) с явной формулировкой «сравнение двух окон на одной сессии не входит в MVP»; живая проверка `92-duplicate-focus`; тесты duplicate/same/unknown и пустой сессии.
- [x] Жизненный цикл безопасен, утечек нет — `91-missing-session-state`, `91-missing-recovery`; тесты отсутствия роста карт/подписок и гонок; закрытие окна освобождает подписки и не удаляет сессию.
- [x] Недоступная сессия не ломает доску — окно остаётся на месте, остальные окна продолжают работать, доска не бросает исключений (живой прогон, `tests/session-bridge.client.spec.ts`).
- [x] Тесты и snapshot replay зелёные — см. §5.
- [x] Задачи Beads 9.1–9.3 закрыты по критериям (закрываются вместе с коммитом этапа).
- [x] Коммит этапа содержит Agent Note — [2026-09-19-ketos-board-session-restore.md](../../../.agents/notes/implemented/architecture/2026-09-19-ketos-board-session-restore.md) (+ zh, i18n-пара).

## 4. Отклонения

- **Найденный живым прогоном дефект исправлен в этапе.** Первая версия guard'ов после `await` сравнивала `record.sessionId !== undefined`, из-за чего восстановление пропавшей сессии через «Новая сессия» молча теряло созданный чат (два осиротевших файла сессий в scratch-доме). Guard'ы `create`/`createChat`/`createChatTarget` теперь сравнивают `sessionId` с его значением до `await`; добавлен регрессионный тест «replaces a missing session when the window starts a new chat».
- **Скриптовый аудит `audit.mjs` не писался.** §4.10 требует пополнять скриптовый аудит для этапов, меняющих геометрию, шкалу или слои; этап 9 таких изменений не вносит. Вместо скрипта — живой MCP-прогон с машинными проверками (`audit.json`/`audit-verdict.json`) и QA-кадрами; при переносе проверок в следующий геометрический этап сценарий переносится копией.
- **GIF не записан.** PR ещё не создавался, а видимое изменение этапа — состояния восстановления/пропажи; кадры и вердикты сохранены в каталоге этапа. При публикации PR GIF снимается по skill `record-browser-gif`.
- **Покрытие `ui-board` — исключение MVP-форка.** `pnpm run test:coverage` (CI-гейт per-file 100 %) в этапе не запускался; `packages/client/ui-board/src/**` исключён политикой MVP-форка в `vitest.config.ts` («MVP-fork coverage policy»), как на этапах 3–8.
- **Новых пакетов нет.** Манифесты, ростер и генераторы не менялись (кроме перегенерированного `slot-catalog.ts` — в нём поехали только номера строк `slots.ts`); `pnpm run doc-sync`, `pnpm run build`, `pnpm run hygiene` прогнаны.
- **Живой прогон не проверил стриминг после восстановления.** В scratch-доме нет провайдера моделей, поэтому «продолжают стримить» подтверждено архитектурно (открытая сессия сохраняет свой `SessionEventStream`; `open()` идемпотентен), а не наблюдением токенов.
- **Вне этапа (не закрыто):** `ketos-c1g` (промоушен пары глифов fullscreen), `ketos-bmz` (доменный граф соседних пакетов) — существующие открытые задачи, этап их не касается.

## 5. Проверки

| Команда | Результат |
|---|---|
| `pnpm exec vitest run packages/client/ui-board/tests` | зелёный; 26 файлов, 272 passed (было 249 после этапа 8) |
| `pnpm exec vitest run packages/ketos/client-locale-ru` | зелёный; 11 passed; корпус `ru-keys.json` дополнен семью ключами |
| `pnpm run test:gui` | зелёный; 404 файла, 5700 passed, 1 skipped |
| `DSH_SNAPSHOT=replay pnpm run test:web` | зелёный; 101 файл passed, 1 skipped, 359 passed, 15 skipped, расхождений нет |
| `pnpm run typecheck` | зелёный; 0 ошибок TS |
| `pnpm run lint` | зелёный |
| `pnpm run duplication` | зелёный; 0 клонов |
| `pnpm run doc-sync` | зелёный; 34 гейта passed, 0 failed (включая translation pairing для README и новой Agent Note) |
| `pnpm run build` | зелёный; 238 клиентских артефактов, 3 публичных значения |
| `pnpm run hygiene` | зелёный; 16 гейтов passed, 0 failed |
| `.playwright-mcp/stage-09-gif/audit/audit-verdict.json` | `{ total: 8, failed: [], ok: true, aborted: false }` — живой прогон на `pnpm ketos web --port 3099` против scratch-дома; кадры `qa/90-restored-windows.png`, `qa/91-missing-session.png` |
| `pnpm exec vitest run packages/client/ui-board/tests --coverage --coverage.include='packages/client/ui-board/src/**/*.{ts,tsx}'` | не запускался: `ui-board` — исключение покрытия MVP-форка (`vitest.config.ts`) |

## 6. Следующий шаг

Этап 10 «Диалог: отправка, стриминг и отмена» продолжает работу в окне: отправка и оптимистичное эхо, стриминг и состояния хода, очередь/стир/отмена, ошибки и история. Входные данные этапа 9 для него: канал окна уже несёт `running`, `chat`, `queue`, `turnError`, `promptError`; мост владеет `windowId → sessionId` и восстанавливает его из `bindings`; правило дублей и состояния `restoring`/`missing` зафиксированы. После приёмки пользователем агент закрывает эпик `ketos-5v2.10` в Beads, создаёт и подготавливает worktree этапа 10 от принятой ветки `stage-09-harness-sessions`.
