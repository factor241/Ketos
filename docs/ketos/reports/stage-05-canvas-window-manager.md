# Отчёт этапа 5. Движок холста и оконный менеджер v1

> Заполнен по шаблону [stage-report-template.md](../stage-report-template.md). Ветка `stage-05-canvas-window-manager`, worktree `/Volumes/Projects/Ketos bot.worktrees/stage-05` (от принятой ветки этапа 4 `stage-04-theme-i18n`, `780780f`). План этапа — `ketos_v7_master_plan/stage-05-canvas-window-manager.md` (ревизия 15); задачи — Beads `ketos-5v2.6.1`–`ketos-5v2.6.5`.

**Демонстрация (GIF).** `.playwright-mcp/stage-05-gif/board-canvas-engine.gif` (1000×626, 12.9 с, 129 кадров, 4.2 МБ; источник 4.6–20.4 с, скорость 1.6×, финальная задержка 3 с) — один непрерывный прогон на живом сервере (`pnpm ketos web` против scratch-дома, порт 3195, без провайдера моделей, модельных вызовов нет): две карточки agent, черновик в composer, Space+драг, зум колесом над холстом/доком и его отсутствие над composer'ом, парковка угла окна под миникартой и рост окна через ручку кольца, culling при уходе вида (пустой холст, окна видны только в миникарте), возврат вида — черновик цел, fullscreen (соседи и хром уходят), Escape. 0 ошибок консоли; сторборд с таймстемпами — `.playwright-mcp/stage-05-gif/storyboard-stage05.txt`, QA-кадры — `.playwright-mcp/stage-05-gif/qa/s5-*.png`.

## 1. Итог этапа

Движок холста и оконный менеджер доведены до бюджетов §II.4: у каждого жеста один владелец жизненного цикла (`pointer-gesture.ts`, `pointercancel` и размонтирование), колесо различает холст/хром и ленту/панель, вьюпорт меряется `ResizeObserver`, окна за пределами видимой области скрываются (`content-visibility`) без потери состояния, кадры окон мемоизированы, z-диапазон окон ограничен под хромом, у закрытого окна освобождается запись моста, а ручки ресайза текущего окна подняты над плавающим хромом (слой-кольцо). 20 окон дают 59.9–60.3 FPS при панорамировании и зуме (порог 55, цель 60), худший кадр 20.9–31.4 мс, длинных задач нет, окно открывается за 14–35 мс до первого кадра (бюджет 100); baseline записан в [perf-baseline.md](../perf-baseline.md). Решение этапа — Agent Note [2026-09-17-ketos-board-gestures-culling](../../../.agents/notes/implemented/architecture/2026-09-17-ketos-board-gestures-culling.md).

## 2. Подэтапы

| Подэтап | Задача Beads | Статус | Подтверждение |
|---|---|---|---|
| 5.1 Математика вьюпорта и жесты | `ketos-5v2.6.1` | выполнен | `tests/canvas.client.spec.tsx` (пан с пустого холста, Space+драг, `Ctrl/Cmd+0`), `tests/pointer-gesture.client.spec.tsx`, `tests/wheel-zoom.client.spec.ts`, `tests/store.client.spec.ts` (зум к курсору и снап); аудит: `36-wheel-priority` (лента не зумит, док и миникарта зумят), `39-wheel-console` без ошибок, `40-viewport-remint`, `41-surface-markers` (по одному маркеру), `44-space-drag-pans`, `45-reset-viewport` |
| 5.2 Ресайз и снап на фактической модели | `ketos-5v2.6.2` | выполнен | `tests/resize.client.spec.ts` (все восемь направлений, пропорциональный угол, снап до клампа, отсутствие уменьшения диагональю), `tests/store.client.spec.ts` (клампы, рост), `window/resize-gesture.ts` — один общий жест для ручек кадра и кольца; аудит: клампы 552/648 (`02`/`03`), пропорция (`11`), кадры узкого и широкого окна (`08`, `12`) |
| 5.3 Z-order, фокус и закрытие | `ketos-5v2.6.3` | выполнен | `tests/store.client.spec.ts` (фокус/порядок, сброс активного окна и режимов при закрытии, `> 90` окон — верх диапазона 99), `tests/slots.client.spec.tsx` (клик по пустому холсту сохраняет активное окно; одно действие Escape за нажатие); аудит: `46-z-ceiling` (`maxWindowZ 31 < 100`, миникарта выше окон), `47-layer-ladder` |
| 5.4 Слои хрома, панель и полноэкранный режим | `ketos-5v2.6.5` | выполнен | `window/HandleRing.tsx` + `tests/slots.client.spec.tsx` (кольцо выше дока, ресайз с кольца, снятие в fullscreen); `isolation: isolate` на корне доски — Stacking-контракт в комментарии `BoardViews.tsx`; аудит: `42`/`43` — все восемь ручек достижимы под омнибоксом и миникартой (`blocked: []`), `49-fullscreen-hidden-neighbours` (соседи скрыты, состояние живо), `ketos-d3a`/`ketos-jdb` закрыты |
| 5.5 Производительность и culling | `ketos-5v2.6.4` | выполнен | `tests/culling.client.spec.ts`, `tests/session-bridge.client.spec.ts` (закрытие окна освобождает запись, сессия жива), `tests/slots.client.spec.tsx` (черновик выживает culling и возврат); аудит: `31`–`34`/`37`/`48` FPS (59.9–60.3), `35`/`36`–`36c` culling и черновик, `50-long-tasks` 0; baseline — [perf-baseline.md](../perf-baseline.md) |

## 3. Критерии приёмки этапа

- [x] Юниты математики вьюпорта и снапа — `pnpm exec vitest run packages/client/ui-board/tests` (16 файлов, 109 тестов) и зум к курсору с клампами 0.2/2.0 в `tests/store.client.spec.ts`.
- [x] Все жесты завершаются общим cleanup, слушатели снимаются при размонтировании — `tests/pointer-gesture.client.spec.tsx` (pointerup, pointercancel, повторный dispose, размонтирование, замена жеста); аудит `44` (Space+драг работает после всех жестов).
- [x] Колесо не пишет ошибок и следует приоритету — аудит `38-wheel-priority` (`overLane: true`, `overDockZooms: true`, `overMinimapZooms: true`), `39-wheel-console` (`passiveError: false`).
- [x] Вьюпорт переизмеряется при изменении геометрии — аудит `40-viewport-remint`: соотношение фрустума к рамке панели 1.000 до и после сворачивания сайдбара, ширина панели 1160 → 1384 px.
- [x] Маркер `data-surface` уникален — аудит `41-surface-markers` (`canvas: 1`, `canvas-layer: 1`); тесты `apply`/`canvas`/`slots` обновлены.
- [x] Space+драг и `Ctrl/Cmd+0` работают — аудит `44-space-drag-pans` (пан 392/−72 → 562/−2), `45-reset-viewport` (зум 1, pan 0/0).
- [x] Ресайз на фактической модели подтверждён юнитами и аудитом — `tests/resize.client.spec.ts`, аудит `02`/`03` (пол 552×648), `11` (пропорция), `08`/`12` (узкое и широкое окно); геометрия — один `window/resize.ts`, жесты — один `window/resize-gesture.ts`.
- [x] z-order/фокус/закрытие консистентны и не конфликтуют с Escape — `tests/store.client.spec.ts`, `tests/slots.client.spec.tsx`; Escape выполняет одно действие в порядке «меню → редактор → панель → fullscreen», окно не закрывает.
- [x] Потолок z-order при > 90 окнах — `tests/store.client.spec.ts` (95 окон: `max z ≤ 99`, порядок сохранён), аудит `46-z-ceiling`.
- [x] Ручки достижимы под хромом, слои зафиксированы — аудит `42`/`43` (`blocked: []`, `raisedByRing` — все восемь), `47-layer-ladder` (`isolation: isolate`, хром 100), `48b-selection-overlay` (оверлей выбора 500 выше хрома 100, капсула кликабельна); `ketos-d3a` закрыт кольцом, `ketos-jdb` — собственным контекстом наложения корня.
- [x] 20 окон ≥ 55 FPS (цель 60) без лишних ре-рендеров; baseline записан — аудит `31`–`34`/`37`/`48` (59.9–60.3 FPS, худший кадр 21–29 мс), `50-long-tasks` 0; [perf-baseline.md](../perf-baseline.md).
- [x] Culling не теряет состояние — аудит `35`–`36c` (20 из 20 окон скрыты, черновик в скрытом окне цел, окно возвращается), `tests/slots.client.spec.tsx`.
- [x] Закрытие окна не удаляет сессию — `tests/session-bridge.client.spec.ts` (после `release` окно не в `windowIds`, сессия в списке), аудит циклами открытия-закрытия (DOM возвращается к базовому числу узлов).
- [x] `pnpm run test:gui`, `DSH_SNAPSHOT=replay pnpm run test:web`, `pnpm run typecheck`, `pnpm run lint`, `pnpm run doc-sync`, `pnpm run hygiene`, `pnpm run duplication` — см. §5.
- [x] PR/коммит этапа содержит Agent Note — [2026-09-17-ketos-board-gestures-culling.md](../../../.agents/notes/implemented/architecture/2026-09-17-ketos-board-gestures-culling.md) (+zh, +i18n.yaml) и обновлённая заметка о fullscreen.

## 4. Отклонения

- **«Уход хрома» заменён кольцом ручек.** Первая реализация 5.4 закрывала `ketos-d3a` тем, что слой хрома (док/омнибокс/миникарта) гаснул и переставал принимать указатель, когда кольцо ресайза активного окна пересекало его измеренный бокс. Живой аудит забраковал это: окно, придвинутое к левому краю, полностью выключало док — по нему нельзя было ни переключить окно, ни добавить новое. Вместо ухода хрома `window/HandleRing.tsx` рисует восемь ручек активного окна в корне доски над хромом (z-index 150, ниже оверлея выбора 500) по той же проекции `translate(pan) scale(zoom)`; неактивные окна сохраняют ручки кадра. Альтернативы и причина отказа записаны в Agent Note.
- **Fullscreen скрывает соседей, а не размонтирует.** Формулировка этапа 4 «слой окон рендерит только это окно» заменена общим правилом «скрывать, не размонтировать» (`isWindowHidden` + `content-visibility`): иначе вход в fullscreen терял черновики остальных окон. Заметка о fullscreen обновлена в том же коммите; `tests/slots.client.spec.tsx` фиксирует 20 смонтированных кадров, 19 скрытых в режиме.
- **Найден и закрыт дефект дока (`ketos-v9m`).** При 20 окнах рейл дока (строка на окно) выше панели: внешние строки и кнопки «добавить»/«сбросить» уходили за вьюпорт и не нажимались (аудит: «element is outside of the viewport»). Рейл получил `max-height: calc(100% - 48px)` и прокрутку с l2-парой скроллбара; строка Beads создана и закрыта в этом этапе.
- **Baseline производительности начат здесь.** Сводный `docs/ketos/perf-baseline.md` по плану — этап 20; запись этапа 5 ведётся в этом файле (метод, до/после, ограничение безголового rAF-потолка 60 Гц).
- **`dsb-client-store` не расширялся под батчинг.** План упоминает «батчинг state-обновлений на кадр»; батчинг уже даёт React 18 в пределах одного обработчика (мув+ресайз), а `raf`-режим движка потребовал бы правки общего пакета без измеримой выгоды — отложено, записано в Agent Note.
- **Ручной замер FPS заменён скриптовым.** §II.4 предполагает ручной замер в dev-консоли; счётчик `requestAnimationFrame` в Playwright-аудите воспроизводит ту же математику и оставляет машинный артефакт (`audit.json`), что и требует задача 5.5(2).
- **Вне этапа (не закрыто):** `ketos-6kt` (тултип остаётся открытым после смены подписи), `ketos-1lx` (заголовки окон держат локаль открытия), `ketos-c1g` (промоушен пары глифов fullscreen), `ketos-bmz` (доменный граф соседних пакетов) — существующие открытые задачи, этап их не касается.

## 5. Проверки

| Команда | Результат |
|---|---|
| `pnpm exec vitest run packages/client/ui-board/tests` | зелёный; 16 файлов, 109 passed |
| `pnpm exec vitest run <спеки> --coverage --coverage.include='packages/client/ui-board/src/**/*.{ts,tsx}'` | см. §5а |
| `pnpm run test:gui` | зелёный; 394 файла, 5540 passed, 1 skipped |
| `DSH_SNAPSHOT=replay pnpm run test:web` | см. §5а |
| `pnpm run build` | зелёный; 238 клиентских артефактов, 3 публичных значения |
| `pnpm run typecheck` | зелёный |
| `pnpm run lint` | см. §5а |
| `pnpm run duplication` | см. §5а |
| `pnpm run doc-sync` | см. §5а |
| `pnpm run hygiene` | см. §5а |
| `verify-translation-pairing` (запись пар) | перезаписаны `ui-board/README`, обе заметки (fullscreen + новая); проверка пар зелёная |
| `.playwright-mcp/stage-05-gif/audit.mjs` | 45 проверок, 0 падений; FPS 59.9–60.3, ручки достижимы, черновики выживают culling и fullscreen |

### 5а. Фактические прогоны

Прогоны на финальном дереве этапа (macOS arm64, Node v26.3.1, pnpm 11.7.0):

| Команда | Результат |
|---|---|
| `pnpm exec vitest run packages/client/ui-board/tests` | зелёный; 16 файлов, 113 passed |
| Покрытие `packages/client/ui-board/src/**` | команда выполняется (`--coverage.include='packages/client/ui-board/src/**'`), но пакет исключён из per-file 100% политикой MVP-форка (`vitest.config.ts`, комментарий «MVP-fork coverage policy»); `WindowFrame.tsx` — 94.6% stmts / 100% lines, `window-screen.ts` — 100% / 50% branch до добавления теста фолбэка, остальные новые модули в отчёте не инструментируются как загруженные через граф слотов |
| `pnpm run test:gui` | зелёный; 394 файла, 5539 passed, 1 skipped |
| `DSH_SNAPSHOT=replay pnpm run test:web` | зелёный; 101 файл, 359 passed, 15 skipped, расхождений нет |
| `pnpm run build` | зелёный; 238 клиентских артефактов, 3 публичных значения |
| `pnpm run typecheck` | зелёный; 0 ошибок TS |
| `pnpm run lint` | зелёный (5 замечаний исправлено: кавычки, скобки стрелок, лишние проверки и утверждения типов) |
| `pnpm run duplication` | зелёный; 0 клонов (1765 файлов) |
| `pnpm run doc-sync` | зелёный; 34 гейта |
| `pnpm run hygiene` | зелёный; 16 гейтов (после `git add` удаления `window/pointer-cleanup.ts` — гейт читает индекс) |
| `pnpm run verify-translation-pairing --write` | перезаписаны пары `ui-board/README`, `2026-09-16-…-window-fullscreen`, `2026-09-17-…-gestures-culling`; парная проверка зелёная |
| `.playwright-mcp/stage-05-gif/audit.mjs` (46 проверок) | зелёный; `31`–`34`/`37`/`48`: FPS 59.9–60.3, худший кадр 20.9–31.4 мс; `30`: открытие окна 14–35 мс; `35`–`36c`: 20/20 окон скрыто, черновик цел и возвращается; `38`/`39`: приоритет колеса и отсутствие ошибок; `40`/`41`: переизмерение вьюпорта и уникальные маркеры; `42`/`43`: все восемь ручек достижимы; `44`/`45`: Space+драг и сброс вида; `46`/`47`: потолок z и лестница слоёв; `48b`: оверлей выбора выше хрома (z 500 против 100) и кликабелен; `49`: fullscreen скрыл соседей; `50`: длинных задач 0 |

## 6. Следующий шаг

После приёмки: эпик этапа (`ketos-5v2.6`) закрывается, worktree этапа 6 создаётся от принятой ветки. Этапу 6 нужны: каркас и хром окна (6.1) — уже общий `WindowFrame` с мемоизацией и кольцом ручек; лента и её состояния (6.2); заголовок окна и название чата (6.3) — открытая задача `ketos-1lx` про локаль заголовков; композер: состояния, границы, политика отправки (6.4) — включая лимиты вложений и отсутствующие `/model` и `/file`. Открытые наблюдения этапа 5 для следующих этапов: док прокручивается при > ~12 окнах (этап 7.1 владеет списком окон и, вероятно, заменит строки), ручки неактивного окна под хромом требуют одного клика по окну (кольцо следует за активным), culling использует фиксированный мировой отступ 480.
