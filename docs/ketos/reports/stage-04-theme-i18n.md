# Отчёт этапа 4. Визуальный язык: тема Harness, CSS Modules, локали

> Заполнен по шаблону [stage-report-template.md](../stage-report-template.md). Ветка `stage-04-theme-i18n`, worktree `/Volumes/Projects/Ketos bot.worktrees/stage-04` (от принятой ветки этапа 3 `stage-03-slots-windows`, `c31a39c`). Отчёт переписан после реворка: пользователь отменил кетосовую палитру и потребовал рисовать доску темой и элементами DeepSeek Harness. Состояние отчёта — финальный коммит этапа (см. §6).

## 0. Доработка после реворка: чат-окно с полным composer

По требованию пользователя («в окне должны быть все элементы чата со скринов; сейчас это обрезок») этап расширен в том же worktree:

- **Сессии в окнах.** `session-bridge.ts` владеет картой `windowId → sessionId`, создаёт сессию при первом показе окна (`sessions.create()` → `open()` → `binding()`) и подписывается на `target('chat')`, `SessionSnapshot` и проекции. Окно фокусируется → сессия становится текущей (осознанный побочный эффект; закрытие окна сессию не удаляет).
- **Полный composer (`window/ComposerBar.tsx`)** повторяет референс на тех же сервисах: чип пресета агента (переключение — только у пустой сессии), `+`-меню поверх каталога команд (`remote.commands.list`) с собственным пунктом вложений, `/`-попап с подсказками и описаниями, `@`-упоминания (`fileReferences` + `sessionReferenceResolver`), чип разрешений (`/permission`, полный доступ за `RiskConfirmation`), чип модели с подменю уровня рассуждений (`ctx.modelDirectories`), чип плана, контекст-кольцо (`contextPressure`), полосы цели/задач/очереди, кнопки Send/Stop со стиром, вложения-изображения, «Открыть в основной панели».
- **Мост — это и контрольная плоскость:** строка списка сессий (cwd/blank/preset), проекции, модельный каталог, каталог команд, роспись пресетов (лок. через общий `@deepseek-ai/dsh-agent-presets/display`) и причина блокировки композера подписываются по окну и публикуются в один channel; все мутации — через инжектированные колбэки.
- **Тесты:** `tests/conversation-body.client.spec.tsx` (создание сессии, ошибка, лента, стриминг, отправка, стир, стоп, Shift+Enter, загрузка старых ходов, чипы разрешений/контекста/очереди), `tests/fixtures.client.ts` (bench с дублями sessions/remote/modelDirectories/layout), обновлённые `slots`/`apply`/`roster`/`open-window` спеки.
- **Проверки:** `pnpm exec vitest run packages/client/ui-board/tests` — 8 файлов, 49 passed; `pnpm run test:gui` — 386 файлов, 5475 passed, 1 skipped; `DSH_SNAPSHOT=replay pnpm run test:web` — 101 файл passed, 359 passed / 15 skipped; `pnpm run lint`, `typecheck`, `doc-sync` (34), `hygiene` (16), `verify-client-packages`, `verify-module-graph`, `verify-client-ui-i18n`, `verify-translation-pairing`, `verify-client-catalog` (регенерирован) — зелёные. Живьём проверены чипы, `+`-меню, `/`-меню, смена разрешения (команда доходит до хоста и проекция обновляется), вложение-картинка с активацией отправки и отправка промпта в реальную сессию; GIF — `.playwright-mcp/stage-04-gif/board-window-composer.gif` (1200×750, 19.4 с, 194 кадра, 2.2 МБ; источник 6.5–36.0 с, скорость 1.8×, финальная задержка 3 с; 0 ошибок консоли; модельных вызовов нет — чистый дом без провайдера).

## 0.1. Доработка: адаптивность чат-окна и элементы управления

По замечанию пользователя (на скриншотах: обрезка текста слева, уехавшая за край кнопка отправки, горизонтальный скроллбар, обрезанные выпадающие списки, тултип поверх меню, сжатые чипы) выполнено:

- **Минимальный размер = размер по умолчанию.** `MIN_WINDOW_SIZE` (480×560, из шаблона agent) объявлен в `store.ts` как единственный источник; `clampWindowSize` применяет снап и нижнюю границу, `resizeWindow` использует его. Из обоих каркасов убраны локальные пороги 320/200.
- **Три режима роста.** Рёбра — моно-осевой ресайз; углы — пропорциональный (обе оси на доминирующий коэффициент, противоположный угол фиксирован, уменьшение ниже текущего размера невозможно). Логика вынесена в общий `window/resize.ts` (закрывает дублирование `ketos-4k3`).
- **Списки больше не обрезаются.** Все поповеры переведены на `Menu portal` с `getAnchorRect` от реального триггера, динамические `side`/`align`, тултипы выключаются при открытом меню (был конфликт z-index).
- **Голосовой ввод.** Кнопка-микрофон рядом с `+` (локальный глиф: в `ui-primitives` микрофона нет) с браузерным `SpeechRecognition`, активное состояние и остановка при отправке; пункт «Голосовой ввод» в `+`-меню теперь работает; без API кнопка задизейблена.
- **Компактная раскладка.** Карточка объявлена inline-size-контейнером: ≤430px строка инструментов переносится и правая группа (контекст, модель, send/stop) прижимается к правому краю своей строки, ≤360px чипы теряют подписи, ≤320px скрывается кольцо контекста; все строки переносимые, дети с `min-width: 0`, лента без горизонтального скролла.
- **Проверки.** Юниты `tests/resize.client.spec.ts` (грани, пропорции, пол) и расширенный `tests/store.client.spec.ts`; компонентные — portal-меню и задизейбленный микрофон; скриптовый визуальный аудит `.playwright-mcp/stage-04-gif/audit/` на 480×560 / 1056×720 / 1056×960 со всеми открытыми меню: `laneOverflowX=0`, `cardOverflow=0`, `sendInside/micInside/toolRowInside=true`, все меню внутри вьюпорта, клампы 480/560 подтверждены, пропорциональный рост 480×560 → 624×720 с сохранением пропорции. Открыт баг `ketos-d3a`: плавающий хром (док/омнибокс/миникарта) может накрывать ручки ресайза.
- **Гейты после доработки.** `pnpm run test:gui` — 387 файлов, 5483 passed, 1 skipped; `pnpm run lint`, `pnpm run typecheck`, `pnpm run doc-sync`, `pnpm run hygiene`, `verify-client-ui-i18n` (626 файлов), `verify-translation-pairing` (820 пар) — зелёные. GIF доработки — `.playwright-mcp/stage-04-gif/board-window-resize.gif` (1200×750, 15.5 с, 155 кадров, 1.7 МБ; источник 2.4–14.9 с, скорость 1.25×, финальная задержка 3 с; снят с дерева `9835e3a` на порту 3187 против scratch-дома; 0 ошибок консоли; модельных вызовов нет — в доме нет провайдера): минимум и «отпружинивание» при сжатии, пропорциональный рост углом, `/`-меню и пресет через portal, подсказка микрофона, рост по ширине и длинный черновик с активной отправкой. QA-кадры — `.playwright-mcp/stage-04-gif/qa/f3-*.png`, storyboard — `storyboard-resize.txt`.

## 0.2. Доработка: масштаб окна

По требованию пользователя («масштаб со скриншота — базовый, но шрифты крупнее; масштабировать всё окно») выполнено:

- **Одна шкала окна.** `BoardViews.module.css` объявляет на корне доски блок метрик: `--board-font-title` (17px), `--board-font-content` (17px, лента/composer/тело tool-окна), `--board-font-label` (15px, чипы/статусы/поповеры), `--board-font-hint` (14px), `--board-line-input` (24px), `--board-pad-header/window/lane/card`, `--board-gap-window/row`, `--board-control` (32px), `--board-control-lane` (30px), `--board-control-small` (26px), `--board-control-send` (36px), `--board-chip-pad`. Все 16 объявлений `font-size` и зависимые метрики в `AgentCard`/`ToolWindow`/`ConversationBody`/`ComposerBar` читают этот блок; литеральными остались только разовые размеры (вложения, кольцо, радиусы).
- **Пороги компактности** выросли вместе со шкалой: 430/360/320 → 495/415/370 (значения базового дизайна, умноженные на шкалу).
- **Минимум и дефолт окна** — 552×648 (базовые 480×560 на шкале, по сетке 24px: 23×24 и 27×24); обновлены `BOARD_WINDOW_TEMPLATES.agent`, `MIN_WINDOW_SIZE`, спеки `resize`/`store`/`open-window`/`slots`/`conversation-body`.
- **Не масштабируются** (сознательно): хром доски (док, омнибокс, миникарта) и портальные `Menu`/`Tooltip` — они принадлежат приложению, а не окну; ассистентский Markdown остаётся на оси контента приложения (`--dsh-content-font-size`) — ограничение записано в README пакета.
- **Проверки (коммит 1).** `pnpm exec vitest run packages/client/ui-board/tests` — 9 файлов, 57 passed; `pnpm run lint`, `typecheck`, `doc-sync`, `hygiene` — зелёные; живой аудит на 552×648 и скриншоты — в §0.3.

## 0.3. Доработка: кнопка в шапке и полноэкранный чат

По требованию пользователя («кнопку открыть — в правый верхний угол; по нажатию просто полноэкранный режим чата; подпись — „открыть во весь экран“») выполнено:

- **Кнопка переехала в шапку окна** (правый верхний угол, рядом с заголовком; закрытие осталось слева), из ленты убран плавающий кружок; глиф входа — общий `IconFullscreenOutline16`, выхода — локальный `window/fullscreen-glyph.tsx` (в общем наборе нет; промоушен — отдельная задача).
- **Смысл заменил прежний переход в основную панель.** Новое состояние стора `fullscreenWindowId` + действия `setWindowFullscreen`/`exitFullscreen`; `closeWindow` сбрасывает режим. Прямоугольник окна остаётся авторитетным: выход возвращает ровно ту геометрию, что была.
- **Отрисовка.** Кадр получает `inset: 0` и `data-board-fullscreen`, класс `.fullscreen` убирает радиус и тень; канвас на время режима публикует нулевой pan и `zoom: 1` (иначе `inset` внутри трансформированного `.surface` масштабируется), колесо мыши не зумит; слой окон рендерит только полный экран, `BoardRoot` не рендерит док/омнибокс/миникарту, ручки ресайза скрыты, drag шапки отключён; Escape и та же кнопка выходят из режима. Тул-окна остаются без режима (их тела ещё не существуют).
- **Мёртвый путь удалён:** `BoardWindowInjected.openInMainPanel`, метод моста, и `layout` из `inject` (сервис больше не используется) — контракт не несёт второго способа покинуть доску.
- **Тесты.** `tests/store.client.spec.ts` (вход/экзит/закрытие/неизвестный id/неизменная геометрия) и `tests/slots.client.spec.tsx` (инлайн `inset: 0`, скрытые соседи и хром, нулевой pan/zoom, восстановление по Escape, переключение из шапки) — 10 файлов, 70 passed вместе с локалью.
- **Agent Note** — `2026-09-16-ketos-board-window-fullscreen.md` (+zh, +i18n.yaml): режим является стор-переходом, а не инжектированным колбэком; прежняя заметка о сессиях обновлена и перекрёстно связана (частичная суперсессия, обе активны).
- **Проверки.** Живой аудит `.playwright-mcp/stage-04-gif/audit.mjs` (порт 3187, scratch-дом, сборка `pnpm run build`): кегли окна `title/lane/input/chip = 15/15/15/14px`, контролы шапки 32 и отправки 36; клампы 648/552; пропорция 552×648 → 696×816 сохранена; все меню внутри вьюпорта; `sendInside/micInside/toolRowInside=true`, `laneOverflowX=cardOverflow=0`; fullscreen `fillsPanel=true`, слои `['canvas']`, после Escape `restored=true` и слои `['canvas','dock','omnibar','minimap']`. GIF — `.playwright-mcp/stage-04-gif/board-window-fullscreen.gif` (1200×750, 15.9 с, 159 кадров, 2.3 МБ; источник 2.6–16.4 с, скорость 1.3×, финальная задержка 3 с; 0 ошибок консоли; модельных вызовов нет — в доме нет провайдера). Кадры `qa/f4-*.png`, storyboard `storyboard-fullscreen.txt`. Наблюдение вынесено в баг `ketos-6kt`: тултип кнопки, чья подпись меняется по клику, остаётся открытым (обход в записи — перезагрузка страницы между дублями); ещё один баг — `ketos-zz0` (колесо мыши зумит вместо прокрутки ленты вне fullscreen).


## 0.4. Доработка: чипы в одну строку, серединный эллипсис пути, кегль +10%

По замечаниям пользователя («две кнопки должны быть в одной плоскости — одна слева, другая справа; скрытие пути должно приходиться на слово Project, чтобы название помещалось; увеличь шрифт примерно на 10%») выполнено:

- **Контекстная строка — одна линия.** `.contextRow` переведён на `flex-wrap: nowrap` и `justify-content: space-between`: путь занимает свободное место (`flex: 1 1 auto`), чип пресета прижат вправо (`margin-left: auto`) и сжимается только после пути. Заодно удалены мёртвые `@container`-правила для чипов — они целились в `.cwdChip`/`.presetChip`, но контейнер объявлен на `.card`, а строка является его сиблингом, поэтому не срабатывали никогда.
- **Серединный эллипсис пути.** Новый `window/path-label.ts`: чистый `elideMiddlePath(path, fits)` идёт по кандидатам от длинного к короткому (`/Volumes/Projects/Ketos bot.worktrees/stage-04` → `/Volumes/…/Ketos bot.worktrees/stage-04` → `…/Ketos bot.worktrees/stage-04` → `/Volumes/…/stage-04` → `…/stage-04` → `stage-04`), то есть первым скрывается сегмент после головного (`Projects`), а имя проекта уходит последним; бюджет берётся из измерения ширины лейбла (ResizeObserver + canvas `measureText` по computed-шрифту), без ResizeObserver рисуется полный путь. Полный путь остаётся в тултипе чипа.
- **Кегль +10%.** Токены окна: title 15→17, body 16→18, content 15→17, label 14→15, hint 13→14, `--board-line-input` 22→24; textarea `min-height` 48→52, `max-height` 180→200; пороги компактности 495/415/370 → 545/455/405. Контролы, паддинги, гэпы и дефолт окна (552×648) не менялись.
- **Проверки (коммит 3).** `pnpm exec vitest run packages/client/ui-board/tests` — 10 файлов, 66 passed (включая 6 юнитов `elideMiddlePath` и компонентный тест чипа пути). Живой аудит: кегли `title/lane/input/chip = 17/17/17/15px`, оба чипа на одной линии (`oneLine=true`, `top` совпадает) во всех состояниях, при 552×648 путь показан как `/Volumes/…/stage-04` (имя целиком), в широком окне — полный путь; остальные проверки (клампы, пропорция, меню, fullscreen) остались зелёными.

## 0.5. Доработка: кнопка «Чаты» и панель навигации по проектам и чатам

По требованию пользователя («кнопка «Чаты» в левой части окна; при нажатии выезжает компактный список чатов из основного окна, 20–30% площади, под окном как нижний слой, адаптивно к ресайзу; в fullscreen — обычный интерфейс чата со списком слева и чатом по центру») выполнено:

- **Панель — спутник кадра, а не второе окно.** Новый keyed-слот `board.window.panel` (ключи `agent`/`clone`); слой окон рендерит его в том же `Fragment` перед кадром, поэтому равный уровень плюс порядок DOM ставят кадр над своей панелью, а панель — над холстом. Своего `z-index` у панели нет, атрибут `data-board-panel`, панель остаётся смонтированной в закрытом состоянии — открытие и закрытие это одна transform-анимация (`--ds-transition-duration-slow`, гасится при `prefers-reduced-motion`).
- **Кнопка в шапке** левее закрытия: глиф `IconPanelLeftOutline16` (тот же, что у кнопки сайдбара приложения), aria `window.chats`, `aria-expanded`; состояние — `panelWindowId` в store (`openWindowPanel`/`closeWindowPanel`, сброс при закрытии окна).
- **Геометрия — функция прямоугольника окна** (`window/panel-geometry.ts`, world-единицы): 26% ширины окна с полом 220px и потолком 320px, высота 92% по центру, 30% собственной ширины уходит под кадр ⇒ видимая полоса ≈20–25% площади; при перемещении/ресайзе/зуме панель следует без кода. В fullscreen панель встаёт встык слева на всю высоту, кадр получает `inset: 0 0 0 <ширина>`, а лента и композер — центрированную колонку 768px (`ConversationBody`).
- **Выбор чата перепривязывает окно:** мост разделён на `create`/`switchTo`/`attach`, добавлены `bind(windowId, sessionId)` (валидация по списку, release, повторный attach) и `createChat(windowId, target)`; канал окна теперь несёт `sessionId`, поэтому панель подсвечивает текущий чат. Панель читает списки через `hooks`-компартмент инъекции (`useSessionList`, `useWorkspaceList`), а `workspaces` добавлен в cordis-inject, devDependencies и tsconfig.
- **Модель списка локальна** (`window/chat-list-model.ts`): группировка по `sessionIds` рабочей папки, скрытие субагентов и архивных, пустая сессия — только у своего окна, сортировка по `updatedAt`, «Без группы» последней; правила сайдбара (`ui-workspace/tree.ts`) не импортируются — это внутренности другого плагина.
- **Проверки (коммит 4):** `panel-geometry` (4 юнита: клампы, оба «подтыка» и overlay, стыковка), `chat-list` (5: группировка, фильтры субагентов/архива/пустой сессии, порядок, «Без группы»), store (панель, закрытие окна), слоты (закрытая панель, геометрия, порядок «до кадра», отсутствие z-index, Escape: панель → fullscreen, стык в fullscreen, перепривязка по клику); всего `vitest` 88 тестов (13 файлов). Живой аудит: панель присутствует и ведёт себя по режимам (`tucked` в окне обычного размера, `overlay` когда окно занимает весь видимый холст, `docked` в fullscreen: `flushLeft/fullHeight/frameStartsAfterPanel/frameFillsRest = true`, колонка чата 768px), слой под кадром подтверждён пробой `elementFromPoint`, закрытие по Escape работает (`17-panel-closed`), порядок Escape «панель → fullscreen» подтверждён. GIF — `.playwright-mcp/stage-04-gif/board-window-chats-panel.gif` (1200×750, 9.5 с, 95 кадров, 1.4 МБ; источник 1.6–11.0 с, скорость 1.15×, финальная задержка 3 с; 0 ошибок консоли; модельных вызовов нет — в доме нет провайдера): окно с кнопкой, выезд панели со списком проектов, чаты проекта, переключение чата (чип каталога следует), закрытие по Escape, стык в fullscreen с центрированной колонкой. Кадры `qa/f5-*.png`, storyboard `storyboard-panel.txt`.

- **Гейты после доработок (коммиты 3–4):** `pnpm run test:gui` — 390 файлов, 5504 passed, 1 skipped; `pnpm run lint`, `pnpm run typecheck`, `pnpm run doc-sync` (34), `pnpm run hygiene` (16), `verify-client-ui-i18n` (631), `verify-translation-pairing` (822), `verify-client-packages`, `verify-client-catalog` (регенерирован) — зелёные; `DSH_SNAPSHOT=replay pnpm run test:web` — 101 файл, 359 passed / 15 skipped.

## 0.6. Доработка: папка уезжает из чат-панели окна

По требованию «убрать выбор папки из основной панели чата и перенести управление папками/ветвлением в боковую панель» сделан первый шаг (полный перенос — отдельные коммиты):

- Из композера окна удалены cwd-чип, его меню (`cwd.pick`) и вся обвязка: `path-label.ts` с измерением и серединным эллипсисом (и его спек) удалены, ключи `cwd.none/choose/pick` убраны из `locale.ts` (zh+en), ru-корпуса и `ru-keys.json`; контекстная строка остаётся однострочной и несёт только чип пресета агента.
- Действие «папка» переехало в панель: в её шапке на уровне проектов появилась кнопка «Добавить папку…» (`panel.addFolder`, глиф `IconProjectAddOutline16`), вызывающая перевод действия `pickWorkspace` (мост пересоздаёт пустую сессию окна в выбранном каталоге).
- В cordis-`inject` доски добавлен `uiWorkspace` (сервис использовался мостом, но не был объявлен — латентная ошибка); бенч тестов получил дубль `uiWorkspace`.
- Тесты: из `conversation-body` убран тест чипа и его стаб, в `slots` проверка перепривязки переехала на маркер текущего чата в панели (`data-board-chat-current`); `audit.mjs` и `record-panel.mjs` больше не ссылаются на чип.
- Ограничение (в README): hero-чип папки в главной панели приложения не тронут — перенос касается окна доски.

## 0.7. Доработка: панель как отдельный слой — рейл, ширина, глубина

- **Панель больше не подтыкается под кадр.** Она выезжает из левого края окна (правого — если слева нет места; при окне во всю видимую доску — overlay внутри его левого края, поверх кадра) и стоит вплотную к кадру на его высоте; слой над холстом, ряды — внутри панели: иерархия «холст → панель → строки».
- **Рейл вместо «просто скрытой» панели:** свёрнутое состояние — компактная полоса 44×170px на том же краю (четыре кнопки 36px: развернуть, новый чат, добавить папку, поиск — последняя придёт в 2b), кнопка сворачивания живёт в шапке панели, кнопка «Чаты» в шапке окна остаётся вторым входом.
- **Ширина изменяемая:** ручка-полоса 8px на внешнем краю, дефолт 300px, клампы 260–420 и не более 45% ширины окна; ширина и признак свёрнутости — в board-store (`panelWidth`, `panelCollapsed`).
- **Почему левая сторона:** это сторона, где живут списки самого приложения; конфликт с хромом доски (док слева, миникарта справа-снизу, z-index 100) решён тем, что док и миникарта уходят, пока панель открыта — их полосы иначе перекрывали внешний край панели и ручку ресайза. В overlay-режиме ручка остаётся на правом краю окна.
- **Проверки:** юниты геометрии (клампы/share, beside справа/слева, overlay, рейл, docked), store (клампы ширины, свёрнутость, сброс при закрытии окна), слоты (рейл, открытие с рейла и из шапки окна, beside-геометрия, drag-ресайз, сворачивание из шапки панели, Escape: панель → fullscreen, docked inset с учётом ширины, рейл исчезает в fullscreen); живой аудит 22 состояния, 0 ошибок: рейл 44×170, beside/right, инлайн-геометрия, ресайз 300→360, docked 768-колонка, полный набор прежних проверок.

## 0.8. Доработка: полный перенос управления папками и ветками в панель

- **Папки:** создание (мини-браузер каталога: по одному уровню, кликабельные крошки, «Новая папка» с инлайн-вводом, «Использовать эту папку» → `workspaces.create`), переименование (инлайн), удаление (подтверждение-инлайн; удаляется только регистрация), порядок (drag по строке + пункты «выше/ниже»); панель показывает только рабочие папки, у которых есть видимый чат, — пустые папки в список не попадают (`window/chat-list-model.ts`, спек `tests/chat-list.client.spec.ts`).
- **Чаты:** создание в проекте с семантикой «переиспользовать пустую» (`startChat`), переименование, **ветка** (`sessions.fork` + привязка ребёнка к окну), архив (с подтверждением), порядок (drag + меню), возраст и точка running, текущий подсвечен.
- **Поиск** (локальный по названию и каталогу) и **меню группировки/сортировки** (по проектам / одним списком; вручную / по обновлению) — состояние в board-store.
- Переброс действий панели в сервисы: `rename/delete/insertBefore` рабочих папок, `listDirectory/createDirectory`, `startChat/renameChat/forkChat/archiveChat/reorderChat`; в инъекцию добавлены `pickDirectory`-семейство и все перечисленные колбэки.
- **Попутно:** Escape в полях и при открытом меню больше не закрывает панель (клавишу забирает редактор/меню); миникарта уходит, пока панель открыта (иначе её угол перехватывал клики по строкам панели); в демо-доме подменён композитор каталога на browse-вариант, иначе `listDirectory` недоступен.
- **Проверки:** 3 новых спеки слотов (переименование/порядок/удаление проекта через меню, ветка+архив чата, регистрация папки через браузер), юниты модели (группировка/порядок/фильтр/анкеры) — 94 теста в ui-board и локали; живой аудит панели — 22 состояния, 0 ошибок (рейл 44×170, beside/right с инлайн-геометрией, ресайз 300→360, docked 768-колонка); живой прогон управления — 12 шагов, 0 ошибок консоли: регистрация папки через браузер, создание/переименование/ветка/архив чата, поиск (фильтр 1 группа), переключение на «один список». GIF — `.playwright-mcp/stage-04-gif/board-panel-management.gif` (1200×750, 2.4 МБ; источник 2.6–23.8 с, скорость 1.45×, финальная задержка 3 с), QA-кадры `qa/f6-*.png`, проверки `checks-panel-manage.json`, сторборд `storyboard-panel-manage.txt`.
- **Гейты после К2б:** `test:gui` 389 файлов / 5509 passed; `lint`, `typecheck`, `doc-sync` (34), `hygiene` (16), `verify-client-ui-i18n` (630), `verify-translation-pairing` (822), `verify-client-packages`, `verify-client-catalog` — зелёные; `DSH_SNAPSHOT=replay test:web` — 101 файл / 359 passed.
- **Ограничение, зафиксированное в README:** в композиции с нативным выбором каталога `listDirectory` недоступен — панельный браузер сообщает об ошибке, а регистрация папки остаётся за штатным флоу приложения; в демо-доме выбор каталога переключён на browse-пару.
- **Задача на будущее (Beads):** фолбэк панельного браузера на нативный выбор каталога, когда browse-возможность не скомпонована.

## 0.9. Доработка приёмки: общий каркас окна и фолбэк выбора папки

Диагностика перед приёмкой нашла два пробела: гейт `pnpm run duplication` был красным (пять клонов в `ui-board`: четыре между `AgentCard.tsx` и `ToolWindow.tsx` ≈ 104 строки — импорты, жест заголовка, ресайз, шапка и нижний блок — и один внутренний в `Minimap.tsx`), а панель «Чаты» умела управлять папками только при browse-пикере: на локальном macOS `dsh-host-directory-picker-auto` монтирует `native`, который не предоставляет `listDirectory`/`createDirectory`, и уровень папок показывал сырую ошибку без какого-либо действия.

**Что сделано.** Один общий каркас `window/WindowFrame.tsx` (+`WindowFrame.module.css`) несёт жест заголовка, восемь ручек ресайза, общий хром (закрытие, заголовок, контекстная шапка) и два контрола, которые предлагают только agent/clone-окна, — `features: { panel?, fullscreen? }`; `AgentCard` стал тонкой обёрткой с обоими флагами, а `index.ts` регистрирует `WindowFrame` напрямую для типов `connectors`/`settings`/`dashboard`/`tasks`, поэтому второй каркас и его CSS-модуль удалены. `Minimap` получил общий шаг `panFromPointer` вместо двух почти одинаковых обработчиков; из шкалы убран неиспользуемый `--board-font-body`. Панель получила инъецированный `pickDirectory`; при недоступном листинге уровень папок показывает локализованное «Просмотр папок недоступен» и кнопку «Выбрать папку в системе…», которая вызывает `uiWorkspace.pickDirectory()` и регистрирует выбранную папку (ключи `panel.browseUnavailable`, `panel.pickFolder` — zh/en/ru + манифест). Совместимость: поведение browse-композиции и геометрия окна не изменились (аудит воспроизвёл те же значения), а нативная композиция проверена живьём.

**Проверки.** `pnpm run duplication` — 0 клонов (1761 файл); `pnpm run test:gui` — 389 файлов, 5510 passed, 1 skipped (на один тест больше: кейс фолбэка в `slots.client.spec.tsx`); `pnpm run typecheck`, `pnpm run lint` — зелёные; `pnpm run verify-client-ui-i18n` — 630 файлов; `pnpm run verify-translation-pairing` — 822 пары; `DSH_SNAPSHOT=replay pnpm run test:web` — 359 passed, 15 skipped без расхождений; живой аудит — 22 состояния с прежними числами (552×648, рейл 44×170, панель 260–420, fullscreen, док-колонка 764px). Нативная композиция (auto-пикер на macOS, беспилотный Chromium): панель показывает оба локализованных элемента, консоль без ошибок (кадр `qa/native-fallback.png`); после проверки в демо-доме восстановлена browse-композиция.

## 1. Итог этапа

Доска перестала быть отдельным визуальным островом. Палитра — только семантические алиасы `ui-theme` (никаких оранжевых/кремовых override-слоёв): холст на `--dsw-alias-bg-base`, окна и док — по рецепту плавающей панели (`bg-layer-2` + `border: 0` + `--dsw-elevation-prominent` + перебиндовка `--dsw-elevation-stroke-color`), фокус — `--dsw-alias-state-business-primary`, разделители — 0.5px-волоски, скролл-пары l2. Хром собран из элементов Harness: иконки `ui-primitives`, `Tooltip` на каждом иконочном контроле, `Menu` для Action Menu, `Tag` для статус-капсул; цветной «светофор» и эмодзи удалены. Мок-контент (фейковые модели, system prompt, тумблеры коннекторов) удалён вместе с ключами словарей, ru-корпусом и манифестом; `board.window.body` теперь предоставляет только `conversation`. Компоненты доски остались разбиты на CSS Modules с инлайном только для геометрии и вычисляемых pan/zoom-переменных; в `packages/client/ui-board/src` нет ни одного литерального цвета. Гейты этапа зелёные.

## 2. Подэтапы

| Подэтап | Задача Beads | Статус | Подтверждение |
|---|---|---|---|
| 4.1 Токены темы | `ketos-5v2.5.1` | выполнен | `ui-theme` снова 6 листов (`ketos-brand.css` удалён, порядок закреплён в `client-styles.client.spec.ts`); grep по `ui-board/src` — 0 hex/rgba; точка сетки — `--dsw-alias-border-l3`; `getComputedStyle` живьём в обеих темах |
| 4.2 CSS Modules и хром из Harness | `ketos-5v2.5.2` | выполнен | 10 модулей; `ui-primitives` (иконки, `Tooltip`, `Menu`, `Tag`); инлайн — только `left/top/width/height/zIndex` и `--board-pan-*`/`--board-zoom`/`--board-grid-*`; `pnpm --filter @deepseek-ai/dsh-client-ui-board bundle` собирает 10 style-инъекторов |
| 4.3 Локализация | `ketos-5v2.5.3` | выполнен | `src/client/locale.ts` (zh/en, 131 ключ), `board-ru.ts` (11 `it` в спеке `client-locale-ru`), `tests/fixtures/ru-keys.json`; `verify-client-ui-i18n` (622 файла на шаге) и `verify-translation-pairing` зелёные; живьём ru → Settings → English |
| 4.4 Гейты и визуальная приёмка | `ketos-5v2.5.4` | выполнен | `canvas`/`apply`/`slots`/`roster`/`open-window`/`inspector` спеки обновлены (11 файлов, 84 passed в `packages/client/ui-board/tests`); `test:gui` 387 файлов / 5483 passed на шаге; snapshot replay без расхождений; чек-лист §4 заполнен; GIF существует |
| 4.5 Адаптивность чат-окна | `ketos-d3a` (наблюдение) | выполнен | минимум = размер по умолчанию, пропорциональный ресайз углом, портальные меню, микрофон, container queries 430/360/320; юниты + компонентные + скриптовый аудит на трёх размерах; GIF `board-window-resize.gif` |
| 4.6 Полноэкранный режим | `ketos-5v2.5.8` | выполнен | кнопка в шапке окна (вход `IconFullscreenOutline16`, выход — локальный глиф), состояние `fullscreenWindowId` в store, нулевой pan/zoom и скрытый хром на время режима, Escape возвращает прежнюю геометрию; `store`/`slots` спеки; Agent Note `2026-09-16-ketos-board-window-fullscreen.md`; GIF `board-window-fullscreen.gif` |
| 4.7 Панель «Чаты» | `ketos-5v2.5.9` | выполнен | навигация по проектам и чатам (`window/chat-list-model.ts`), мини-браузер каталога, создание/переименование/ветка/архив/порядок/поиск/группировка; рейл 44×170, ширина 260–420 (дефолт 300), docked-колонка в fullscreen; список — только папки с видимым чатом; юниты `panel-geometry`/`chat-list` + слоты; GIF `board-panel-management.gif` |
| 4.8 Гейты, аудит и визуальная приёмка | `ketos-5v2.5` (эпик) | выполнен | 10 `*.module.css`, 131 ключ словаря, 11 спек-файлов / 84 `it` в `packages/client/ui-board/tests` плюс 11 `it` в `packages/ketos/client-locale-ru/tests`; `test:gui` 389 файлов / 5510 passed / 1 skipped; `duplication` 0 клонов; живой аудит 22 состояния; проба нативной композиции; эпик закрыт с причиной «Принят» |

## 3. Критерии приёмки этапа

- [x] **Палитра — из DeepSeek Harness, без оранжевого.** В `ui-board/src` нет ни одного литерального цвета; все правила — `--dsw-*`-алиасы; `grep` не находит `ketos-brand`, `board-canvas`, `data-board-surface`. Живьём: в светлой теме доска светлая, в тёмной — тёмная (одна и та же композиция).
- [x] **Элементы доски — из Harness.** Иконки `ui-primitives` вместо эмодзи/самодельных глифов; `Tooltip` на dock-строках, закрытии окна, composer-действии и отправке; Action Menu — `Menu` (портал, `selection="fill"`); статус-капсулы — `Tag` (`success`/`neutral`); фокус-ринги — конвенция Harness. Пустой `Button`/`Switch`/`StateDot` не подключены осознанно (см. §5).
- [x] **Настройки и окно — из Harness.** Мок-панель Settings (фейковые модели, system prompt) и мок-панель Connectors (три фейковых тумблера) удалены вместе с таб-полосой, кнопкой дока, пунктом меню, ключами `tool.*`/`menu.connectors`/`rail.addConnectors`/`canvas.connectorsTitle`/`canvas.messageSent` и ru-переводами; новых заглушек нет. Цветной «светофор» заменён одной кнопкой закрытия (`IconCloseOutline16`); меню `•••` отменено ревизией 14 плана этапа 6.
- [x] **Стили — модульные, инлайн только геометрия/переменные.** JSDoc `DashboardCanvas` фиксирует правило; 10 модулей рядом с компонентами; `clsx` в условных состояниях (фокус окна, активная строка дока, открытое меню, готовность отправки, agent/tool-заливка прямоугольника).
- [x] **Типы модулей работают при сборке** — `src/css-modules.d.ts` в порядке `*.module.css` → `*.css`; `tsc -b tsconfig.client.json` зелёный; бандл собирает CSS.
- [x] **Локализация.** zh/en — словари пакета, ru — корпус `@ketos/client-locale-ru`; `verify-client-ui-i18n` и `verify-translation-pairing` зелёные; переключение языка проверено живьём (ru по умолчанию; Settings → English меняет подписи доски).
- [x] **Гейты.** `pnpm run test:gui` 385 файлов / 5465 passed / 1 skipped; `DSH_SNAPSHOT=replay pnpm run test:web` без расхождений; `typecheck`, `lint`, `build`, `hygiene`, `doc-sync`, `verify-client-catalog` (после `gen-client-catalog`), `verify-client-packages`, `verify-cordis-config` зелёные.
- [x] **Визуальная приёмка и GIF** — чек-лист §4 заполнен; `.playwright-mcp/stage-04-gif/board-harness-theme.gif`.

## 4. Визуальная приёмка (чек-лист)

Наблюдение — живой `pnpm ketos web` (scratch `DSH_HOME`, порт 3185) с дерева финального коммита после `pnpm run build`; кадры в `.playwright-mcp/stage-04-gif/qa/` (светлая и тёмная темы).

| Элемент | Наблюдение | Кадр |
|---|---|---|
| Холст и сетка | поверхность темы (`bg-base`) с точечной сеткой на `border-l3`, видима в обеих темах; зум масштабирует точку и шаг | `01-board-light.png`, `07-board-dark.png` |
| Карточка агента | поверхность `bg-layer-2` с elevation-штрихом; фокус — бизнес-синий; иконка закрытия, `Tag success` «Готово», `Tag neutral` «Изучено»; composer на `specific-input-major` | `02-agent-window.png` |
| Второе окно | та же панельная рецептура, что и у карточки (один хром на все типы) | `03-second-window.png` |
| Док | панель `bg-layer-2`, строки-иконки (`IconAgentPresetOutline16`), `Tooltip` с заголовком, активная строка на `markdown-tag`, reset — `IconFullscreenOutline16` | `04-dock-tooltip.png` |
| Omnibox | composer-рецепт (`specific-input-major` + `elevation-soft` + r22), меню `Menu` с иконками, кнопка отправки на `button-info-fill` | `05-action-menu.png`, `06-sent.png` |
| Инспектор | пунктирная рамка `state-business-primary`, капсула `bg-layer-3` + `IconCloseOutline16` | `08-inspector.png` |
| Тема | светлая и тёмная темы: доска следует палитре, оранжевого нет | `01-board-light.png` / `07-board-dark.png` |
| Язык | ru по умолчанию; Settings → English → подписи доски английские | `09-board-english.png` |

Референс OpenSwarm: каталог `docs/ketos/reference/` в репозитории отсутствует; сверка велась по палитре/композиции и по живым кадрам. Прежний «кетосовский» вид (кремовый холст, тёмные карточки) отменён пользователем осознанно — доска теперь следует теме Harness.

## 5. Отклонения

- **Отмена §4.8 плана и `AUDIT_REPORT.md` (97, 186–188).** План требовал объявить палитру Кетос бренд-слоем в `ui-theme` на селекторе `.board-canvas` и держать «доску в своей палитре Кетос»; пользователь отменил это требование. Решение записано новой Agent Note («доска использует общую тему и контролы Harness»), старая заметка о бренд-слое переведена в `archived/architecture/` (полное замещение, кросс-ссылки починены, печать архивного гейта пройдена).
- **`inject` доски — 13 сервисов, без `layout`.** В коде (`src/client/index.ts:38-42`): `slots`, `locale`, `sessions`, `workspaces`, `uiWorkspace`, `uiConversation`, `modelDirectories`, `remote`, `remote.commands`, `remote.agentPresets`, `remote.goals`, `remote.fileReferences`, `remote.sessionReferenceResolver`. План предписывал «проверить `inject = ['slots', 'layout', 'locale']`»; сервис `layout` доска не читает — регистрация в `main` ждёт декларацию слота, а не сервис, как и у ui-conversation; остальные сервисы пришли с мостом сессий, composer'ом и панелью «Чаты».
- **Мок-панели удалены, а не перекрашены.** Реальных полей (модель, пресет, cwd) в этапе 4 нет: их источники — `ctx.modelDirectories`, `remote.session.modelCatalog/selectModel`, `remote.agentPresets.*` — принадлежат этапам 11/13, а секции настроек Harness нельзя смонтировать вне панели, которая их объявляет; клиентского редактора system prompt в Harness нет вовсе. `WindowKind`/`WindowBodyKind` и регистрации кадров сохранены, но ни один вход не открывает пустые окна.
- **Одна кнопка закрытия вместо меню `•••`.** Цветной «светофор» базового `fe89719` заменён иконочным контролом (`IconCloseOutline16`).
- **Меню `•••` отменено ревизией 14 плана.** Файл плана этапа 6 больше не планирует меню «закрыть/свернуть/дублировать» в шапке окна: хедер несёт закрытие, «Чаты», заголовок и полноэкранный режим, а статус выражается лентой и чипами, не капсулой.
- **Цвета без прямых алиасов получили семантическое соответствие:** мини-карта — `state-business-primary` (агент) и `label-caption` (прочее), фрустум — бизнес-тинт; статус-капсулы — `Tag` (`success`/`neutral`). Новых `--dsw-*`-токенов и бренд-переменных не добавлено; точка сетки больше не исключение — она на `border-l3`.
- **`ui-primitives` подключён, но не целиком.** Использованы 10 иконок (`IconCloseOutline16`, `IconPlusOutline16`, `IconFullscreenOutline16`, `IconAgentPresetOutline16`, `IconBrowseOutline16`, `IconPaperclipOutline16`, `IconGlobeOutline14`, `IconInspectOutline12`, `IconSparkle16`, `IconSendOutline16`), `Tooltip`, `Menu`, `Tag`. `Button` не подходит для круглых иконочных контролов (в каталоге нет icon-only варианта — это зафиксированное ограничение пакета), `Switch` и `StateDot` остались без потребителя после удаления мок-контента. Пакет уже был в devDependencies доски — новых зависимостей не добавлено.
- **Каталог слотов регенерирован.** `gen-client-catalog` обновил `slot-catalog.ts`: `board.window.body` теперь сообщает один занятый ключ (`conversation`) и один occupant.
- **ru-манифест обновлён вручную.** `sync-dictionaries.mjs` требует корпус community-пака (`--corpus`, `--community`), недоступный офлайн; ключи namespace `board` в `tests/fixtures/ru-keys.json` приведены к новому набору тем же правилом, что применяет скрипт (ключи словаря = ключи корпуса), и спек `locale-ru` это подтверждает.
- **Поведенческое следствие реворка:** «тёмная карточка» больше не тёмная в светлой теме — окна следуют поверхности темы. Это и есть требование пользователя; в README это отражено как свойство, а не как ограничение.
- **Латентный дефект типов CSS Modules исправлен по ходу** (порядок ambient-деклараций в `src/css-modules.d.ts`), иначе модули не типизировались.
- **Открытые пробелы:** `ketos-1lx` (заголовки окон не перелокализуются), `ketos-3kf` (passive-listener колеса — этап 5.1), `ketos-e9s`, `ketos-4k3`, `ketos-0s0`; базовые проблемы (`verify-client-domain-graph` по unrelated upstream-пакетам, `ketos-bmz`).

## 6. Проверки

| Команда | Результат |
|---|---|
| `pnpm exec vitest run packages/client/ui-board/tests` | зелёный: 11 файлов, 84 passed |
| `pnpm exec vitest run packages/client/ui-board/tests packages/ketos/client-locale-ru/tests` | зелёный: 12 файлов, 95 passed (84 в ui-board + 11 в ru-локали) |
| `pnpm exec vitest run packages/client/ui-theme/tests packages/ketos/client-locale-ru/tests` | зелёный: 12 файлов, 93 passed |
| `pnpm run test:gui` | зелёный: 389 файлов, 5510 passed, 1 skipped |
| `DSH_SNAPSHOT=replay pnpm run test:web` | зелёный: 101 файл passed, 1 skipped; 359 passed, 15 skipped; расхождений нет |
| `pnpm run typecheck` | зелёный: host-сборка + `tsc -b tsconfig.client.json` |
| `pnpm run lint` | зелёный: oxlint 1.76.0, 0 diagnostics на 3605 файлах |
| `pnpm run build` | зелёный на чистом дереве финального коммита: `238 client artifact(s)` |
| `pnpm run doc-sync` | зелёный: 34 passed |
| `pnpm run hygiene` | зелёный: 16 gates passed |
| `pnpm run verify-client-ui-i18n` | зелёный: 630 файлов |
| `pnpm run verify-translation-pairing` | зелёный: пары консистентны (README/заметки перезаписаны) |
| `pnpm run verify-client-catalog` | зелёный после `gen-client-catalog` (body-ключи доски: `conversation`) |
| `pnpm run verify-client-packages` | зелёный: 52 пакета |
| `pnpm run verify-cordis-config` | зелёный: 143 конфига |
| `pnpm run duplication` | зелёный: 0 клонов (1761 файл, 376 003 строки) — после выноса общего каркаса окна |
| `pnpm run verify-archived-agent-notes` | зелёный: 1887 артефактов, 3 новых печати |
| `pnpm --filter @deepseek-ai/dsh-client-ui-board bundle` | зелёный: 10 style-инъекторов (10 `*.module.css`) |
| Живой `pnpm ketos web` (scratch `DSH_HOME`, порт 3185) | холст/сетка/окно/док/омнибокс/миникарта/инспектор по чек-листу §4 в светлой и тёмной темах; ru и en; консоль без ошибок (два известных предупреждения `ketos-3kf`); сервер остановлен |
| Живой аудит раскладки `audit.mjs` (порт 3187, scratch-дом) | зелёный: 22 состояния, 0 ошибок консоли — 552×648 и клампы, рейл 44×170, панель 300→360 (клампы 260–420), docked-колонка 764px, fullscreen |
| Проба нативной композиции (auto-пикер каталога на macOS, беспилотный Chromium) | зелёный: панель показывает локализованные «Просмотр папок недоступен» и «Выбрать папку в системе…», консоль без ошибок (кадр `.playwright-mcp/stage-04-gif/qa/native-fallback.png`) |
| GIF-запись `record-browser-gif` | `.playwright-mcp/stage-04-gif/board-harness-theme.gif` — 1200×750, 14.7 с, 147 кадров, 3.2 МБ; источник 9.5–30.6 с, скорость 1.8×, финальная задержка 3 с; снят с финального коммита, порт 3186; storyboard, QA-кадры (`qa/01-board-light.png` … `qa/09-board-english.png`), `console-errors.log` и исходный WebM рядом; вызовов модели нет (окна доски — заглушки до этапов 9/10) |

## 7. Следующий шаг

Этап 5 — «Движок холста и оконный менеджер v1» (математика вьюпорта и жесты, ресайз и снап на фактической модели, z-order и закрытие, слои хрома, панель и полноэкранный режим, производительность и culling), затем этап 6 — состояния оболочки окна и ленты (каркас и заголовок уже общие; меню `•••` и статус-капсула отменены). Предпосылки выполнены: палитра и контролы общие с Harness, поэтому правки стилей в этапах 5–19 не задевают доску как исключение, а новые окна наследуют хром; строки по-прежнему владеются словарями. Эпик `ketos-5v2.5` закрыт в Beads с причиной «Принят»; worktree этапа 5 создан и подготовлен от этого коммита — `/Volumes/Projects/Ketos bot.worktrees/stage-05` (ветка `stage-05-canvas-window-manager`, HEAD `e76a4fe`).

## 8. Коммиты этапа

`git log --oneline c31a39c..e76a4fe` — 24 коммита, сгруппированы по смыслу:

| Группа | SHA | Что сделано |
|---|---|---|
| Тема, каркас и локали — первая редакция (отменена) | `4292283`, `a1fe6b9` | скоуп-бренд-слой Кетос `ketos-brand.css` в `ui-theme`, первые словари zh/en и ru-корпус, первый отчёт о теме и локалях; отменено реворком |
| Реворк: тема Harness и локали | `6e7eca5`, `82de616`, `d8731d6` | доска на общей теме и контролах Harness, `ketos-brand.css` удалён, словари пересобраны; починка ссылок архивной заметки; фиксация гейтов и GIF в отчёте |
| Composer окна | `6f62a78`, `1cb9bc4`, `9cd69a8` | полный composer поверх Harness-сервисов, обновление каталога и манифестов, запись проверок и GIF в отчёт |
| Адаптивность | `9835e3a`, `fa02f1a` | пол окна = размер по умолчанию, пропорциональный угловой ресайз, портальные меню, микрофон; числа и GIF в отчёте |
| Шкала интерфейса | `efec14d` | единая шкала `--board-*`, стартовый размер и пол 552×648 |
| Полноэкранный режим | `3fe1ba6` | режим из шапки окна, состояние в store, Agent Note |
| Чипы строки контекста | `950a8a8` | одна строка чипов, серединный эллипсис пути, кегль +10% |
| Панель «Чаты» | `952e5cf`, `c2fba45`, `44b3f9c`, `1661766`, `0fdd83f`, `ca39b38`, `71008d7` | навигация v1, запись чисел и GIF, снятие чипа папки, рейл/ширина/слой, полный менеджмент папок и чатов, возврат панели на левую кромку |
| Приёмка | `15f0651`, `9a6982c`, `e76a4fe` | общий `WindowFrame` для всех типов окон, фолбэк нативного выбора папки, запись приёмки в отчёте |
| Трекер (вне `ui-board`) | `3a53a7f` | `docs/ketos/beads.md` — трекер переведён на stage-04 |

Вне `ui-board` коммиты задевают: `3a53a7f` — трекер/Beads (`docs/ketos/beads.md`); `4292283` и `6e7eca5` — README и стили `ui-theme` (`ketos-brand.css` добавлен и затем удалён); `6f62a78`, `1cb9bc4`, `952e5cf` — `packages/client/ui-board/package.json` и `pnpm-lock.yaml`. Коммит первой редакции палитры Кетос — `4292283`; он отменён реворком `6e7eca5`, который удалил бренд-слой и вернул доску на общую тему Harness.
