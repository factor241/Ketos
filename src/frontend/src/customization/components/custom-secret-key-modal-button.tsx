export const SecretKeyModalButton = ({
  userId,
  modalProps,
  disabled,
}: {
  userId: string;
  modalProps;
  disabled?: boolean;
}): JSX.Element => {
  return <></>;
};

export default SecretKeyModalButton;
