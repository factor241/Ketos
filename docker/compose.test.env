# Deliberately non-secret values for deterministic `docker compose config` tests.
KETOS_DATABASE_URL=postgresql://ketos:ketos@postgres:5432/ketos
KETOS_SUPERUSER=ketos
KETOS_SUPERUSER_PASSWORD=ketos-test-only-password
KETOS_CONFIG_DIR=/var/lib/ketos
