/**
 * Russian dictionary for the `settings.locale` namespace (the Language row of
 * the Settings General section); keys mirror
 * `@deepseek-ai/dsh-client-locale`'s settings dictionaries.
 */
export const ru = {
  'language.title': 'Язык',
}
