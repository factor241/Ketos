# Этап 17. Память клона

Отчёт по §II.4 общего плана: выполненные подэтапы, подтверждённые критерии, отклонения и следующий шаг. Источник — `stage-17-clone-memory.md` (ревизия 17).

## 1. Итог этапа

Клон получил долговременную память: таблица `memories` с FTS5-индексом в том же `clones.db` (шаг v3 forward-only раннера), инструменты `clone_memory_remember`/`clone_memory_search` в scope сессии клона, стабильная секция `clone:profile` и динамический снимок `clone:memory` в промпте, маршрут `/api/ketos.memory` и вкладка «Память» в окне клона. Ветка — `stage-17-clone-memory`, worktree — `/Volumes/Projects/Ketos bot.worktrees/stage-17`, база — принятая `stage-16-clone-interview`. Финальный коммит — в конце этапа. После этапа клон «помнит, кто он», сохраняет и находит факты, а пользователь видит и правит память в окне клона.

## 2. Подэтапы

| Подэтап | Задача Beads | Статус | Подтверждение |
|---|---|---|---|
| 17.1 Таблица памяти и FTS5 | `ketos-5v2.18.1` | выполнен | `pnpm exec vitest run packages/ketos/clone-core/tests` — зелёный, включая `tests/memory.spec.ts` (запись/поиск/обновление/удаление, русская морфология и регистр, изоляция клонов, микро-бенчмарк 10 000 записей ≤ 20 мс) |
| 17.2 Инструменты remember/search | `ketos-5v2.18.2` | выполнен | `tests/composition.spec.ts` — реальный Loader + AgentLoop: инструменты видны только в scope привязанного агента, `remember` пишет, `search` находит, лимиты и отсутствие привязки отклоняются с `ketos/not-a-clone-session`/`ketos/invalid-memory`; результаты несут отрендеренный текст |
| 17.3 Инъекция профиля и памяти | `ketos-5v2.18.3` | выполнен | `tests/composition.spec.ts` — секция `clone:profile` и контекст `clone:memory` в сборке промпта; правка профиля и правка/удаление памяти пользователем доходят до следующей сборки; `renderPrompt` не меняется при записи памяти; `Config` `memoryEntries`/`memoryChars` соблюдаются |
| 17.4 Окно памяти | `ketos-5v2.18.4` | выполнен | `packages/client/ui-board/tests` — зелёный: вкладка «Память» (`clone-window-bar.client.spec.tsx`), тело памяти (фильтр/поиск/правка/удаление — `clone-memory.client.spec.tsx`), сквозной сценарий с заглушкой `/api/ketos.memory` (`clone-flow.client.spec.tsx`); маршрут — `tests/memory-routes.spec.ts` |

## 3. Критерии приёмки этапа

- [x] Память сохраняется/ищется/редактируется — `tests/memory.spec.ts` (репозиторий), `tests/memory-routes.spec.ts` (маршрут), `clone-memory.client.spec.tsx` и `clone-flow.client.spec.tsx` (UI).
- [x] Инъекция работает — `tests/composition.spec.ts`: профиль и снимок памяти в сборке промпта привязанного агента, обновление после правки пользователя, отсутствие инъекции у непривязанной сессии.
- [x] FTS-бюджет соблюдён — микро-бенчмарк на 10 000 записей в `tests/memory.spec.ts` (медиана пяти прогонов < 20 мс).
- [x] Тесты зелёные — `pnpm run test:gui`, `pnpm exec vitest run packages/ketos`, `DSH_SNAPSHOT=replay pnpm run test:web` (см. §5).
- [x] Чужие клоны не видят память друг друга — `tests/memory.spec.ts` и `tests/memory-routes.spec.ts`.
- [x] Definition of Done §II.4: `pnpm run test:gui` зелёный; `DSH_SNAPSHOT=replay pnpm run test:web`; `pnpm run doc-sync && pnpm run build && pnpm run hygiene`; Agent Note в коммите этапа.

## 4. Отклонения

- **Решение 17.4 «окно против вкладки»** зафиксировано в пользу вкладки: `bodyKind: 'clone-memory'` живёт в существующем окне клона (`kind: 'clone'`) как третья вкладка `CloneWindowBar`. Новый `WindowKind` не вводился: он потребовал бы регистраций frame/panel/title/dock без новой возможности, а вкладка переиспользует `cloneId`, привязку сессии и раскладку окна. Сводка памяти в панели «Чаты» не добавлялась — план допускал любой из вариантов, а вкладка является и просмотром, и правкой.
- **Синхронизация FTS** — в репозитории (в одной транзакции), без триггеров, как требует план; `deleteClone` удаляет память клона вместе с привязками, чтобы память не переживала клон.
- **`candidate` — только статус**: `methodology_candidate: true` пишет `status='candidate'`; карантин и PII-маскинг — post-MVP, как и записано в Known Limitations пакета.
- **Поиск лексический**: FTS5 + префиксы из процитированных токенов; семантический поиск/`sqlite-vec` не добавлялись (отложено мастер-планом).
- **У памяти нет revision-CAS**: правки last-writer-wins, окно перечитывает список после каждой мутации. Это осознанное отличие от записи клона, зафиксировано в README и Agent Note.
- **Покрытие**: `packages/ketos/clone-*/src/**` и `packages/client/ui-board/src/**` остаются в исключениях MVP-политики (`vitest.config.ts`); вместо процентов — поведенческие сьюты, перечисленные выше. Комментарий политики не менялся.
- **Новых задач Beads не создавалось**; открытых вопросов, требующих решения пользователя, нет.

## 5. Проверки

| Команда | Результат |
|---|---|
| `pnpm exec vitest run packages/ketos/clone-core/tests` | зелёный; 8 файлов, 96 тестов |
| `pnpm exec vitest run packages/client/ui-board/tests` | зелёный; 37 файлов, 485 тестов |
| `pnpm exec vitest run packages/ketos` | зелёный; 9 файлов, 107 тестов |
| `pnpm run test:gui` | зелёный; 415 файлов, 5916 тестов (1 skipped) |
| `DSH_SNAPSHOT=replay pnpm run test:web` | зелёный |
| `pnpm run typecheck` | зелёный |
| `pnpm run lint` | зелёный |
| `pnpm run duplication` | зелёный; 0 клонов |
| `pnpm run doc-sync` | зелёный; 34 гейта |
| `pnpm run build && pnpm run hygiene` | зелёный; 16 гейтов |
| `pnpm run verify-client-ui-i18n` | зелёный |
| Живая проверка (собранный `ketos web`, Playwright/Chromium) | 10/10 проверок `ok`, вердикт `audit-verdict.json`; см. §3a |

### 3a. Живая проверка (собранный `ketos web`, Playwright/Chromium)

Прогон на живом сервере: `pnpm ketos web --port 3126 --no-open` против чистого дома `DSH_HOME=/tmp/ketos-stage17-home`, `DEEPSEEK_API_KEY=dummy`, `DEEPSEEK_BASE_URL=http://127.0.0.1:3998` — локальная OpenAI-совместимая SSE-заглушка (`.playwright-mcp/stage-17-clone-memory/sse-stub.mjs`, каталог gitignored), которая играет интервьюера и затем вызывает `clone_memory_remember`, `clone_memory_search` и `clone_draft_save`; каждый запрос к модели записан в `requests.jsonl`. Машиночитаемые проверки — `audit/audit.json`, вердикт — `audit/audit-verdict.json` `{ total: 10, failed: [], ok: true, aborted: false }`; кадры — `qa/170…175-*.png`.

| Проверка | Наблюдение |
|---|---|
| Третья вкладка «Память» | Окно клона несёт `profile`/`interview`/`memory`; вкладка открывает тело памяти |
| Пустое состояние | Фильтры «Активная (0)»/«Кандидат»/«В архиве», поле поиска, «В этом статусе ничего нет.» (после аудита; кадр `qa/176`) |
| Запись агентом | `clone_memory_remember` сохранил одну запись (`active`, теги `стиль`, `отчётность`); карточка вызова видна в ленте |
| Поиск агентом | `clone_memory_search` вернул `[id] Предпочитает короткие письма и точные цифры со ссылкой на источник.` |
| Профиль в промпте | Каждый запрос привязанной сессии несёт `You are the digital clone of …` |
| Память в контексте | Запись появилась в runtime-context сообщении следующего запроса |
| Список в окне | Статус, источник (`session-…`), время обновления, текст и теги отображены |
| Правка пользователя | После правки следующий запрос несёт «…отчёты — по пятницам.» в новейшем снимке |
| Удаление пользователя | `memories` = 0 и `memories_fts` = 0; новейший снимок больше не содержит запись |
| Инструменты остаются у сессии | После сохранения профиля в каталоге сессии по-прежнему `clone_memory_remember`/`clone_memory_search` |

Живой прогон дефектов не нашёл.

## 3b. Независимая перепроверка (аудит)

Этап перепроверен двумя независимыми аудитами (хост-пакет и клиентский слой) с попытками опровержения; подтверждённые находки закрыты в том же этапе.

| Находка | Что было не так | Исправление |
|---|---|---|
| `500` вместо `400` на NUL в запросе | Токен с управляющим символом обрезал MATCH-выражение в C-строке SQLite, ошибка FTS5 уходила мимо доменных кодов | `memoryMatchExpression` отклоняет управляющие символы (`ketos/invalid-memory` → 400); тест в `memory.spec.ts` |
| Отказ поиска создавал `clones.db` | `query: '()'` отклонялся уже после ленивого открытия БД, вопреки контракту «валидация до открытия» | Маршрут строит MATCH-выражение до открытия; тест «refuses an unsearchable query without opening the database» |
| Разрыв суррогатной пары при обрезке снимка | Обрезка записи/бюджета могла разрезать пару и вынести одиночный суррогат в запрос модели | `cutTo` не режет пару; тесты на смещениях 380–400 и на общий бюджет |
| Тег с запятой молча перезаписывался | Редактор разделяет теги запятыми, поэтому хранимый тег с запятой не переживал round-trip | Хранилище отклоняет запятую в теге; тест в `memory.spec.ts`, README |
| Неудачное чтение показывалось как «память пуста» | `loadMemories`/`searchMemories` глушили отказ в пустой список, тело рисовало пустое состояние | Чтения возвращают исход (`MemoryReadOutcome`), тело показывает строку отказа с «Повторить»; тест в `clone-memory.client.spec.tsx` |
| Отказ мутации стирался следующим чтением | Успешное чтение сбрасывало флаг, а ветка `missing` показывала уведомление, которое тут же гасло | Чтение больше не сбрасывает уведомление; `missing` просто перечитывает список; тест «keeps a refused mutation visible» |
| Отказ хоста выглядел как «повторите» | `400` от маршрута отображался как retryable failure; границы тегов не проверялись в редакторе | Исход `invalid` с отдельным текстом и клиентская проверка границ; тесты |
| Правка, набранная во время сохранения, терялась | Поля оставались активными при `busy`, патч снимался по клику | Поля и пилюли отключаются на время запроса |
| Счётчик активного фильтра и пустая подпись врали | Во время загрузки показывался счёт прошлого фильтра; пустой отфильтрованный список назывался «память пуста» | Счётчик скрыт во время загрузки; подпись «В этом статусе ничего нет.» для непустого поиска |
| Дата форматировалась локалью браузера | `Intl.DateTimeFormat(undefined, …)` давал смешанный язык интерфейса | Возраст записи через `relativeTime` + словарные ключи `time.*`, как в панели «Чаты» |
| Повторный жест чтения ничего не делал | Повторный submit/клик по активному фильтру не перезапускал чтение — не было способа обновить список | Оба жеста поднимают `epoch`; тесты |
| Тело поиска не проверялось на проводе | `memory:search` не проверялся в сквозном тесте | `clone-flow.client.spec.tsx` проверяет тело запроса поиска |

## 6. Следующий шаг

Этап 18 («Методология и навыки клона») строится на этой базе: `clone_tasks` добавится шагом v4 того же раннера, а статус `candidate` и `skills` клона получат потребителей. После приёмки: закрыть эпик `ketos-5v2.18` в Beads, создать worktree `stage-18-*` от принятой ветки `stage-17-clone-memory`, подготовить окружение и передать пользователю.
