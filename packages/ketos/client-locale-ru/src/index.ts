/**
 * Ketos Russian language pack, Host half: the package has no Host-side
 * behavior; this entry exports the function plugin the Loader contract
 * requires while the client half rides the web roster.
 * @module @ketos/client-locale-ru
 */
/** No-op Host half: all behavior registers in the browser face. */
export function apply(): void {}
