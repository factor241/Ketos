/**
 * Ketos Russian language pack, Host half: the package has no Host-side
 * behavior; this entry exists so the package exports a node face and its
 * client half rides the web roster.
 * @module @ketos/client-locale-ru
 */
export {}
